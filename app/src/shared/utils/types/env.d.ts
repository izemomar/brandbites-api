export type TEnv = {
  NODE_ENV: string;
  DB_USERNAME: string;
  DB_DATABASE: string;
  DB_PASSWORD: string;
  DB_HOST: string;
  DB_PORT: number;
  SERVER_PORT: number;
  ALLOWED_ORIGINS: string;
};
